// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: photon/voice/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "photon.voice.v1";

export enum CallDirection {
  CALL_DIRECTION_UNSPECIFIED = 0,
  CALL_DIRECTION_INCOMING = 1,
  CALL_DIRECTION_OUTGOING = 2,
  UNRECOGNIZED = -1,
}

export function callDirectionFromJSON(object: any): CallDirection {
  switch (object) {
    case 0:
    case "CALL_DIRECTION_UNSPECIFIED":
      return CallDirection.CALL_DIRECTION_UNSPECIFIED;
    case 1:
    case "CALL_DIRECTION_INCOMING":
      return CallDirection.CALL_DIRECTION_INCOMING;
    case 2:
    case "CALL_DIRECTION_OUTGOING":
      return CallDirection.CALL_DIRECTION_OUTGOING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CallDirection.UNRECOGNIZED;
  }
}

export function callDirectionToJSON(object: CallDirection): string {
  switch (object) {
    case CallDirection.CALL_DIRECTION_UNSPECIFIED:
      return "CALL_DIRECTION_UNSPECIFIED";
    case CallDirection.CALL_DIRECTION_INCOMING:
      return "CALL_DIRECTION_INCOMING";
    case CallDirection.CALL_DIRECTION_OUTGOING:
      return "CALL_DIRECTION_OUTGOING";
    case CallDirection.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Keepalive signal for streaming RPCs. Clients should silently discard. */
export interface Heartbeat {
}

/**
 * Opaque cursor for resumable event streams. Clients must treat the value
 * as opaque and pass it back verbatim.
 */
export interface StreamCursor {
  value: string;
}

function createBaseHeartbeat(): Heartbeat {
  return {};
}

export const Heartbeat: MessageFns<Heartbeat> = {
  encode(_: Heartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Heartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Heartbeat {
    return {};
  },

  toJSON(_: Heartbeat): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<Heartbeat>): Heartbeat {
    return Heartbeat.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<Heartbeat>): Heartbeat {
    const message = createBaseHeartbeat();
    return message;
  },
};

function createBaseStreamCursor(): StreamCursor {
  return { value: "" };
}

export const StreamCursor: MessageFns<StreamCursor> = {
  encode(message: StreamCursor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamCursor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamCursor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamCursor {
    return { value: isSet(object.value) ? globalThis.String(object.value) : "" };
  },

  toJSON(message: StreamCursor): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<StreamCursor>): StreamCursor {
    return StreamCursor.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamCursor>): StreamCursor {
    const message = createBaseStreamCursor();
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
